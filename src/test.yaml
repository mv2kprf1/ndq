source: example_source_conn_name
schemas:
  - schema: test_schema_name
    tables:
      table_1:
        no_missing_ids:
          mostly: 0.9 # 90% records must pass this check         
          sql: |
            SELECT * FROM test_schema_name.table_1 t WHERE t.id IS NULL

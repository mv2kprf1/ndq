source: example_source_conn_name
schemas:
  - schema: test_schema_name
    tables:
      table_1:
        expect_no_unexpected_rows:
          columns: ["test_column_1"]
          mostly: 0.9
          sql: |
            select 1 = 1
        another_check:
          columns: ["col_2"]
          threshold: 0.8
      table_2:
        some_other_check:
          columns: ["id"]
